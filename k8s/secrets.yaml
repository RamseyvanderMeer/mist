apiVersion: v1
kind: Secret
metadata:
  name: mist-secrets
type: Opaque
stringData:
  # Replace with real values before apply, or use Secret Manager / sealed-secrets.
  database-url: "REPLACE_WITH_DATABASE_URL"
  sambanova-api-key: "REPLACE_WITH_SAMBANOVA_API_KEY"
  chromadb-api-key: "REPLACE_WITH_CHROMA_DB_API_KEY"
  chromadb-tenant: "REPLACE_WITH_CHROMA_DB_TENANT"
