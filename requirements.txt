# Core
numpy>=1.24.0
PyYAML>=6.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
tenacity>=8.0.0

# Embeddings & ML
torch>=2.0.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Vector store
chromadb>=0.4.0
chroma-mcp  # MCP server for Cursor (run via project venv, not uvx)
pydantic-settings>=2.0.0  # Required for ChromaDB Python 3.14 compatibility

# Knowledge graph
networkx>=3.0

# CLI
typer>=0.9.0

# Scraper (bmwfault.codes mapping fetcher)
playwright>=1.40.0  # also run: playwright install chromium
requests>=2.28.0    # Capsolver API

# LLM (OpenAI for matcher eval --use-llm; Gemini for /query, /clarify)
openai>=1.0.0
google-generativeai>=0.3.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
slowapi>=0.1.9
redis>=5.0.0
python-jose[cryptography]>=3.3.0
pyjwt>=2.8.0
google-auth>=2.29.0  # Google Sign-In ID token verification (mobile / SPA)
passlib[bcrypt]>=1.7.4
